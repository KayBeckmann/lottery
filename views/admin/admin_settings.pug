extends admin_layout

block admin_content
  h1.mb-4 #{pageTitle}

  .card
    .card-body
  //- Flash-Nachrichten (wenn connect-flash eingerichtet ist)
  //- if success_msg && success_msg.length > 0
  //-   .alert.alert-success #{success_msg}
  //- if error_msg && error_msg.length > 0
  //-   .alert.alert-danger #{error_msg}

  p Diese Einstellungen dienen als Standardwerte beim Erstellen neuer Ziehungen oder für andere globale Aspekte der Anwendung.

  form(action="/admin/settings" method="POST")
    .form-group
      label(for="default_max_tickets_per_draw") Standardmäßige maximale Anzahl an Tickets pro Ziehung:
      input.form-control(type="number" id="default_max_tickets_per_draw" name="default_max_tickets_per_draw" value=settings.default_max_tickets_per_draw || 100 min="1")

    .form-group
      label(for="default_ticket_price") Standard-Ticketpreis in BTC (z.B. 0.001):
      input.form-control(type="text" id="default_ticket_price" name="default_ticket_price" value=settings.default_ticket_price || '0.001' pattern="[0-9]+(\\.[0-9]+)?")

    .form-group
      label(for="default_processing_fee_btc") Standard-Bearbeitungsgebühr in BTC (z.B. 0.0001):
      input.form-control(type="text" id="default_processing_fee_btc" name="default_processing_fee_btc" value=settings.default_processing_fee_btc || '0.0001' pattern="[0-9]+(\\.[0-9]+)?")

    //- Weitere globale Einstellungen hier...

    button.btn.btn-primary(type="submit") Einstellungen speichern

  hr
  p
    a(href="/admin") Zurück zum Admin Dashboard